---
    kind: PersistentVolumeClaim
    apiVersion: v1
    metadata:
      name: zabbix-db-storage-pvc
    spec:
      storageClassName: "nfs"
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 100Mi
